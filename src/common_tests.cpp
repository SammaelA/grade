#include "common_tests.h"
#include "tinyEngine/engine.h"
#include "graphics_utils/image_metrics.h"
#include "graphics_utils/modeling.h"

void perform_tests_common(const std::vector<int> &test_ids)
{
  for (int i = 0; i < 80; i++)
    debug("#");
  debug("\nCOMMON RECONSTRUCTION TESTS\n");
  for (int i = 0; i < 80; i++)
    debug("#");
  debug("\n");
}