#include "equation_solvers.h"

std::vector<std::pair<float, float>>
solver::find_intervals(const std::vector<float>& coefs, const float& x1, const float& x2)
{
    std::vector<std::pair<float, float>> intervals;

    return intervals;
}

float 
solver::f(const std::vector<float>& coefs, const float& x)
{
    return 0.f;
}

