#pragma once

enum InputCommands
{
    IC_GEN_HMAP,
    IC_COMMANDS_COUNT
};

enum GenerationCommands
{
    GC_GEN_HMAP,
    GC_COMMANDS_COUNT
};

enum RenderCommands
{
    RC_UPDATE_HMAP,
    RC_COMMANDS_COUNT
};