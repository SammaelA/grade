#!/bin/bash
until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_1.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_2.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_3.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_4.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_5.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_6.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_7.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_8.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_9.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_10.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_11.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_12.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/cup_13.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/vase_1.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/vase_2.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/vase_3.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/vase_4.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/vase_5.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/vase_6.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/vase_7.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/vase_8.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/vase_9.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/pot_1.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/pot_2.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/pot_3.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/pot_4.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/pot_5.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/pot_6.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/pot_7.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/pot_8.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_1.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_2.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_3.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_4.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_5.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_6.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_7.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_8.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_9.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_10.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done

until ./main -sandbox -opt_benchmark diff_optimization_benchmark.blk resources/textures/DishesData/plate_11.jpg
do
    if test -f "config/backup.blk"
    then
        echo "FALL"
    else
        break
    fi
done
